#!/usr/bin/env bash
# Displays the number of occurrences of each IP and HTTP code in apache-acc
awk '{ count[$1" "$9]++ }
END {
    for (key in count)
        print count[key], key
}' access.log | sort -rn


